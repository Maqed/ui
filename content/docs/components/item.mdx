---
title: Item
description: A versatile component for displaying content with media, title, description, and actions.
base: base
component: true
---

<ComponentPreview name="item-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<CodeBlockCommand command="npx shadcn@latest add @herocn/item" />

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

<CodeBlockCommand command="npm install @base-ui/react" />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="item" title="components/ui/item.tsx" />

<Step>Add the following css colors & utilities</Step>
<RenderCSSShared
  light={["--surface", "--surface-secondary", "--surface-tertiary"]}
  dark={["--surface", "--surface-secondary", "--surface-tertiary"]}
  theme={["--color-surface", "--color-surface-secondary", "--color-surface-tertiary"]}
  css={["@utility focus-ring"]}
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import {
  Item,
  ItemActions,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
```

```tsx
<Item>
  <ItemMedia variant="icon">
    <Icon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Title</ItemTitle>
    <ItemDescription>Description</ItemDescription>
  </ItemContent>
  <ItemActions>
    <Button>Action</Button>
  </ItemActions>
</Item>
```

## Composition

Use the following composition to build an `Item`:

```text
ItemGroup
└── Item
    ├── ItemHeader
    ├── ItemMedia
    ├── ItemContent
    │   ├── ItemTitle
    │   └── ItemDescription
    ├── ItemActions
    └── ItemFooter
```

## Item vs Field

Use `Field` if you need to display a form control such as a checkbox, input, radio, or select.

If you only need to display content such as a title, description, and actions, use `Item`.

## Variant

Use the `variant` prop to change the visual style of the item. In addition to outline and muted, herocn includes `default` (surface + shadow), `secondary`, `tertiary`, and `transparent`.

<ComponentPreview name="item-variants" />

## Size

Use the `size` prop to change the padding and density of the item. Available sizes are `default`, `sm`, and `xs`.

<ComponentPreview name="item-sizes" />

## Examples

### Icon

Use `ItemMedia` with `variant="icon"` to display an icon.

<ComponentPreview name="item-icon" />

### Avatar

Use `ItemMedia` with the default variant to wrap an `Avatar` or other custom media.

<ComponentPreview name="item-avatar" />

### Image

Use `ItemMedia` with `variant="image"` for square, cropped thumbnails (for example album art or list thumbnails).

<ComponentPreview name="item-image" />

### Group

Use `ItemGroup` to group related items. It sets `role="list"` and adjusts spacing when items use smaller sizes.

<ComponentPreview name="item-group" />

### Header

Use `ItemHeader` for full-width content above the main row (for example a cover image or banner).

<ComponentPreview name="item-header" />

### Link

Use the `render` prop so the root becomes a link (or button). Focus and hover styles apply to that element.

<ComponentPreview name="item-link" />

```tsx
<Item render={<a href="/dashboard" />}>
  <ItemMedia variant="icon">
    <HomeIcon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Dashboard</ItemTitle>
    <ItemDescription>Overview of your account and activity.</ItemDescription>
  </ItemContent>
</Item>
```

### Dropdown

Compose a compact `Item` inside menu rows—for example, avatar plus text in a `DropdownMenuItem`.

<ComponentPreview name="item-dropdown" />

## RTL

To enable RTL support in herocn, see the [RTL configuration guide](/docs/rtl). Directional icons such as chevrons should use `rtl:rotate-180` where appropriate.

<ComponentPreview name="item-rtl" direction="rtl" />

## API Reference

### Item

<ApiRefTable
  props={[
    {
      name: "variant",
      type: '"default" | "secondary" | "tertiary" | "outline" | "muted" | "transparent"',
      defaultValue: '"default"',
    },
    {
      name: "size",
      type: '"default" | "sm" | "xs"',
      defaultValue: '"default"',
    },
    {
      name: "render",
      type: "React.ReactElement",
      description:
        "Optional element (for example `<a />` or `<button />`) merged onto the root via Base UI `useRender`.",
    },
  ]}
/>

### ItemGroup

Container with `role="list"` and vertical spacing. Accepts standard `div` props.

### ItemSeparator

Horizontal separator between items, built on the `Separator` component.

### ItemMedia

<ApiRefTable
  props={[
    {
      name: "variant",
      type: '"default" | "icon" | "image"',
      defaultValue: '"default"',
      description:
        "`icon` sizes nested SVGs; `image` applies fixed dimensions and clipping for thumbnails.",
    },
  ]}
/>