---
title: Toggle Group
description: A set of two-state buttons that can be toggled on or off.
---

<ComponentPreview name="toggle-group-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<CodeBlockCommand command="npx shadcn@latest add https://herocn.dev/r/toggle-group.json" />

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

<CodeBlockCommand command="npm install @base-ui/react" />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toggle-group" title="components/ui/toggle-group.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"
```

```tsx
<ToggleGroup>
  <ToggleGroupItem value="a">A</ToggleGroupItem>
  <ToggleGroupItem value="b">B</ToggleGroupItem>
  <ToggleGroupItem value="c">C</ToggleGroupItem>
</ToggleGroup>
```

## Composition

Use the following composition to build a `ToggleGroup`:

```text
ToggleGroup
├── ToggleGroupItem
└── ToggleGroupItem
```

## Examples

### Sizes

Use the `size` prop to change the size of the toggle group items.

<ComponentPreview name="toggle-group-sizes" />

### Spacing

Use the `spacing` prop to add gaps between toggle group items.

<ComponentPreview name="toggle-group-spacing" />

### Vertical

Use `orientation="vertical"` for a vertically stacked toggle group.

<ComponentPreview name="toggle-group-vertical" />

### Disabled

<ComponentPreview name="toggle-group-disabled" />

### Custom

<ComponentPreview name="toggle-group-custom" />

## RTL

<ComponentPreview name="toggle-group-rtl" direction="rtl" />

## API Reference

### ToggleGroup

<ApiRefTable
  props={[
    {
      name: "variant",
      type: '"default" | "ghost"',
      defaultValue: '"default"',
      description: "Controls the visual style applied to all items in the group.",
    },
    {
      name: "size",
      type: '"default" | "sm" | "lg" | "icon" | "icon-sm" | "icon-lg"',
      defaultValue: '"default"',
      description: "Controls the size applied to all items in the group.",
    },
    {
      name: "spacing",
      type: "number",
      defaultValue: "0",
      description: "Gap between toggle group items in spacing units. When 0, items are joined together as a single pill.",
    },
    {
      name: "orientation",
      type: '"horizontal" | "vertical"',
      defaultValue: '"horizontal"',
      description: "Controls the layout direction of the toggle group.",
    },
    {
      name: "multiple",
      type: "boolean",
      defaultValue: "false",
      description: "When true, multiple items can be pressed simultaneously.",
    },
    {
      name: "value",
      type: "string[]",
      description: "The controlled pressed values of the group.",
    },
    {
      name: "defaultValue",
      type: "string[]",
      description: "The initial pressed values of the group (uncontrolled mode).",
    },
    {
      name: "onValueChange",
      type: "(value: string[]) => void",
      description: "Callback fired when the pressed values change.",
    },
    {
      name: "disabled",
      type: "boolean",
      defaultValue: "false",
      description: "Disables all items in the group.",
    },
  ]}
/>

### ToggleGroupItem

<ApiRefTable
  props={[
    {
      name: "value",
      type: "string",
      required: true,
      description: "A unique value identifying this item within the group.",
    },
    {
      name: "disabled",
      type: "boolean",
      defaultValue: "false",
      description: "Disables this individual item.",
    },
  ]}
/>
